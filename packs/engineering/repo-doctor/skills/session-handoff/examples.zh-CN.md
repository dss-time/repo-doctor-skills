# 示例

- “当前会话快满了，为完成验证创建 standard 交接。”围绕验证任务整理，并保存到唯一系统临时路径。
- “已有规格、ADR、Issue、Commit 和 Diff 只引用，不复制。”包含改动文件、实际命令、结果、阻塞和下一 Skill。
- 安全临时存储不可用时，在响应中输出完整脱敏交接，并将文件创建标为 `Blocked`。
- 反例：“提交这些改动并发送给另一个开发者。”本 Skill 不 commit，也不发送外部消息。
