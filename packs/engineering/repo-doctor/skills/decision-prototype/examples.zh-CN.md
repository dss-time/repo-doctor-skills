# 示例

- 逻辑原型：“用可丢弃原型判断审批状态模型是否允许一条非法转换。”明确非法转换和判定条件，使用内存合成数据并展示所有相关状态。
- UI 原型：“正式实施前，比较两种结构明显不同的密集设置流程。”所有写操作使用 stub，并按事先声明的可用性判据判断。
- 阻塞：“用生产数据库和客户令牌让原型更真实。”不得连接，应要求获批的非生产替代。
- 交接：即使结论成立，也要转给正式规格或实施；原型文件不能原样升级为生产代码。

反例：“实现并发布已经选定的 UI。”应使用相应规格、计划和实施工作流。
