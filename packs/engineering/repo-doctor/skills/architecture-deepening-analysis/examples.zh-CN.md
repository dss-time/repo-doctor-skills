# 示例

- “三个调用方都在重复把同一领域状态转成相同传输结构，每次变更都要改三处。分析深化方案，但不要重构。”追踪调用方，比较至少两个方向，并保留兼容与回滚。
- “这个文件有 2000 行，把它拆得更干净。”文件大小本身不足以形成候选；除非调用方、职责、变更或测试证据证明摩擦，否则返回“无合格候选”。
- “现在重写整个子系统。”不得执行，应转给澄清、影响分析、计划和明确写授权。

反例：“把已经选定的架构决定记录下来。”应使用 `architecture-decision-record`。
