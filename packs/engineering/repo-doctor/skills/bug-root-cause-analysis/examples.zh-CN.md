# 示例

用户：“可选 tags 为空时保存接口才返回 500。请复现并找根因，不要修。”

预期：先定义成败信号并证明它对应报告故障，再最小化复现、验证替代假设、区分触发条件/直接原因/系统性根因，并报告置信度和修复方向。

audit 模式包含每条命令、工作目录、退出码、权限判断和证据状态。没有合格信号时必须保持推断或未验证。

反例：“帮我检查这个 PR 有没有 Bug。”应使用 `safe-code-review`。
