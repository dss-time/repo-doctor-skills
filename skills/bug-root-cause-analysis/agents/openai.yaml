interface:
  display_name: "Bug Root Cause Analysis（Bug 根因分析）"
  short_description: "Bug 根因分析；默认 fast，允许隐式调用"
  default_prompt: "使用 $bug-root-cause-analysis 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
