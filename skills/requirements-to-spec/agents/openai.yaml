interface:
  display_name: "Requirements Specification（需求规格化）"
  short_description: "需求规格化；默认 fast，允许隐式调用"
  default_prompt: "使用 $requirements-to-spec 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
