interface:
  display_name: "Spec to Work Items（规格拆分工作项）"
  short_description: "规格拆分工作项；默认 fast，允许隐式调用"
  default_prompt: "使用 $spec-to-work-items 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
