interface:
  display_name: "Dead Code Verification（死代码验证）"
  short_description: "死代码验证；默认 fast，允许隐式调用"
  default_prompt: "使用 $dead-code-verification 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
