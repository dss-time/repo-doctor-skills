interface:
  display_name: "Safe Change Plan（安全变更计划）"
  short_description: "安全变更计划；默认 fast，允许隐式调用"
  default_prompt: "使用 $safe-change-plan 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
