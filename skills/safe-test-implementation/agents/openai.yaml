interface:
  display_name: "Safe Test Implementation（安全补充测试）"
  short_description: "安全补充测试；默认 fast，允许隐式调用"
  default_prompt: "使用 $safe-test-implementation 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
