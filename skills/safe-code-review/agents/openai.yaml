interface:
  display_name: "Safe Code Review（安全代码审查）"
  short_description: "安全代码审查；默认 fast，允许隐式调用"
  default_prompt: "使用 $safe-code-review 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
