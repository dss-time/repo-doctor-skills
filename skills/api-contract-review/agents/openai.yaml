interface:
  display_name: "API Contract Review（API 契约审查）"
  short_description: "API 契约审查；默认 fast，允许隐式调用"
  default_prompt: "使用 $api-contract-review 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
