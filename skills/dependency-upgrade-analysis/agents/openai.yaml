interface:
  display_name: "Dependency Upgrade Analysis（依赖升级分析）"
  short_description: "依赖升级分析；默认 standard，仅显式调用"
  default_prompt: "使用 $dependency-upgrade-analysis 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: false
