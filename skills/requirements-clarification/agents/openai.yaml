interface:
  display_name: "Requirements Clarification（需求决策澄清）"
  short_description: "需求决策澄清；默认 fast，允许隐式调用"
  default_prompt: "使用 $requirements-clarification 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
