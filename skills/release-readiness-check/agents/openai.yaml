interface:
  display_name: "Release Readiness Check（发布就绪检查）"
  short_description: "发布就绪检查；默认 standard，仅显式调用"
  default_prompt: "使用 $release-readiness-check 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: false
