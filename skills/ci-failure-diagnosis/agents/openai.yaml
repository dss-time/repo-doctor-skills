interface:
  display_name: "CI Failure Diagnosis（CI 失败诊断）"
  short_description: "CI 失败诊断；默认 fast，允许隐式调用"
  default_prompt: "使用 $ci-failure-diagnosis 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
