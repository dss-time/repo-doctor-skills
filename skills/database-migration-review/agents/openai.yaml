interface:
  display_name: "Database Migration Review（数据库迁移审查）"
  short_description: "数据库迁移审查；默认 standard，仅显式调用"
  default_prompt: "使用 $database-migration-review 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: false
