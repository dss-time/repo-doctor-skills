interface:
  display_name: "Decision Prototype（决策原型验证）"
  short_description: "决策原型验证；默认 standard，仅显式调用"
  default_prompt: "使用 $decision-prototype 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: false
