interface:
  display_name: "Architecture Decision Record（架构决策记录）"
  short_description: "架构决策记录；默认 standard，仅显式调用"
  default_prompt: "使用 $architecture-decision-record 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: false
