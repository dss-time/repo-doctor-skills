interface:
  display_name: "Documentation Sync（文档同步维护）"
  short_description: "文档同步维护；默认 fast，允许隐式调用"
  default_prompt: "使用 $documentation-sync 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
