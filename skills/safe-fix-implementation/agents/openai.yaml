interface:
  display_name: "Safe Fix Implementation（最小安全修复）"
  short_description: "最小安全修复；默认 fast，允许隐式调用"
  default_prompt: "使用 $safe-fix-implementation 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
