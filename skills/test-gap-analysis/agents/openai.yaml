interface:
  display_name: "Test Gap Analysis（测试缺口分析）"
  short_description: "测试缺口分析；默认 fast，允许隐式调用"
  default_prompt: "使用 $test-gap-analysis 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
