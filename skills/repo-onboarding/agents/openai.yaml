interface:
  display_name: "Repo Onboarding（仓库理解）"
  short_description: "仓库理解；默认 fast，允许隐式调用"
  default_prompt: "使用 $repo-onboarding 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
