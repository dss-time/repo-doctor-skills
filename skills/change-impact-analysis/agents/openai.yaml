interface:
  display_name: "Change Impact Analysis（变更影响分析）"
  short_description: "变更影响分析；默认 fast，允许隐式调用"
  default_prompt: "使用 $change-impact-analysis 执行这个有边界的 Repo Doctor 任务。"
policy:
  allow_implicit_invocation: true
